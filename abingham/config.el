;; therapy
;; traad
(configuration-layer/declare-layers
 '(
   auto-completion
   ;; better-defaults
   c-c++
   csharp
   clojure
   dockerfile
   elixir
   emacs-lisp
   fsharp
   git
   go
   html
   markdown
   org
   python
   ;; (shell :variables
   ;;        shell-default-height 30
   ;;        shell-default-position 'bottom)
   ;; spell-checking
   syntax-checking
   version-control
   themes-megapack
   ycmd
   ))
